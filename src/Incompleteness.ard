\import Consistent
\import Logic
\import Representation
\import proof.PrfCompleteness
\import proof.Proof
\import syntax.Context
\import syntax.Syntax

\func Incompleteness
  {_ : Language}
  (T : Theory Context.empty)
  -- assumptions
  (rep : RepresentsInSelf T)
  (con : Consistent T)
  -- ...
  : \Sigma (f : Sentence) (Not (Prf T f)) (Not (Prf T (notH f)))
  => ({?},
      {?},
      {?})